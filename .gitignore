# ignore the output directory
# This will ignore the `dist` directory and all its contents
dist

# ignore the build directory
# This will ignore the `build` directory and all its contents
build

# ignore the node_modules directory
# This will ignore the `node_modules` directory and all its contents
node_modules

# ignore the pnpm lock file
pnpm-lock.yaml

# This file is generated by the TypeScript compiler and contains information about the build state of the project.
tsconfig.tsbuildinfo

# ignore the output zip files
*.zip
./**/*.zip